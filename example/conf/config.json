{
    "listenAddress": ":8080",
    "account": {
        "region": "us-east-1",
        "akid": "{{ .akid | decrypt }}",
        "secret": "{{ .secret | decrypt }}"
    },
    "token": "{{ .apiToken | decrypt }}",
    "logLevel": "info",
    "org": "{{ .org }}"
}
